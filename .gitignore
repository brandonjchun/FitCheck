# --- Course material (not part of the deliverable) ---
fitcheck-spec-v2.md

# --- Private working notes: known gaps and deferred decisions ---
PROJECTLIMITATIONS.md

# --- Private working notes: why the code looks the way it does ---
DECISIONS.md

# --- Python ---
.venv/
venv/
env/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# --- Secrets / config ---
.env
.env.*
!.env.example

# --- Node / frontend ---
node_modules/
dist/
dist-ssr/
.vite/
*.local

# --- Local test fixtures (resumes contain personal data) ---
# Keep sample documents out of the repo. Commit a redacted fixture
# under tests/fixtures/ deliberately if you need one.
/samples/
/uploads/
*.resume.pdf
*.resume.docx

# --- Logs ---
*.log
logs/

# --- Editors ---
# Ignore .vscode/* (not .vscode/) so the two files below can be re-included --
# git cannot un-ignore a file whose parent directory is itself excluded.
.vscode/*
# Keep the shared workspace config: it pins the interpreter to backend/.venv
# so a fresh clone doesn't silently run against a global Python.
!.vscode/settings.json
!.vscode/launch.json
.idea/
*.swp

# --- Claude Code ---
# Per-machine settings only. A shared .claude/settings.json stays committable;
# the .local.json sibling is personal and does not belong in the repository.
.claude/settings.local.json

# --- OS ---
Thumbs.db
desktop.ini
.DS_Store
